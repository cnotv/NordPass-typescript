export enum Routes {
  Login = '/login',
  PasswordHealth = '/items',
  Weak = '/items/weak',
  Reused = '/items/reused',
  Old = '/items/old',
  Root = '/',
}

export enum API {
  Login = 'api/login',
  Items = 'api/items',
  User = 'api/user',
}
